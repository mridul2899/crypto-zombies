pragma solidity >=0.5.0 <0.6.0;

contract ZombieFactory {
    uint256 dnaDigits = 16;

    // Arithmetic operators supported in Solidity include +, -, /, *, %
    // Exponential operator: **

    uint256 dnaModulus = 10**dnaDigits;
}
