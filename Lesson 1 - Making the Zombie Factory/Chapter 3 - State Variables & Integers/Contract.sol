pragma solidity >=0.5.0 <0.6.0;

contract ZombieFactory {
    // State variables are permanently stored in contract storage, i.e., written to Ethereum blockchain.
    // uint refers to uint256 by default and should be generally used.
    uint256 dnaDigits = 16;
}
